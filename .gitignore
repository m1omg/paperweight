# Screenshots are regenerated by tools/shots.js
shots/

# Archival art. Both are derived and rebuildable (see assets/_raw/README.md),
# and together they are ~94 MB against a 5 MB game — too much to carry in a
# repo that GitHub Pages serves. They stay on the local disk.
assets/_raw/
assets/_lossless/
